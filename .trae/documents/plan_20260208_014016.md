配置 `slug` 只是设定了文档的访问网址。`_category_.json` 是用来控制左侧菜单栏的行为（比如点击是展开还是跳转）和排序的。

如果您删除了 `_category_.json`，菜单栏的排序可能会乱，而且点击菜单可能会变成“展开子项”而不是“直接跳转到文档”。

**建议保留** **`_category_.json`**，我会帮您修改它的配置，让它直接指向文档，达到您想要的效果。这样既保持了目录结构整洁，又实现了点击即达。

接下来的操作计划：

1. **删除索引文件**：

   * 删除 `c:\Users\46907\develop\docs\docs\（电脑版）控制客户端\index.md`。

2. **配置文档 URL (Slug)**：

   * 编辑 `c:\Users\46907\develop\docs\docs\（电脑版）控制客户端\新版电脑客户端使用指南.md`。

   * 在文件头部添加 Front Matter 配置 `slug: /pc-client`。

3. **更新分类配置**：

   * 编辑 `c:\Users\46907\develop\docs\docs\（电脑版）控制客户端\_category_.json`。

   * 将 `link` 类型修改为 `doc`，并指向 `新版电脑客户端使用指南`。

