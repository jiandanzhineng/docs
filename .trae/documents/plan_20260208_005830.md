我已分析脚本 `tool/translate-i18n-json.js`，确认其当前逻辑为**原地翻译**（读取目标路径文件 -> 翻译 -> 写回目标路径）。

为了实现**从 `zh-Hans` 翻译**的需求，我将进行以下修改：

1.  **修改任务生成逻辑 (Task Generation)**：
    *   不再检查目标文件是否存在，改为检查 `i18n/zh-Hans/`（即默认语言）下的**源文件**是否存在。
    *   生成的任务对象将同时包含 `srcFilePath`（源文件路径，如 `zh-Hans/code.json`）和 `destFilePath`（目标文件路径，如 `en/code.json`）。

2.  **修改翻译核心函数 (`translateChromeI18nFile`)**：
    *   读取 `srcFilePath` 的内容发送给 AI。
    *   在写入前，自动创建 `destFilePath` 所在的目录（如果不存在）。
    *   将翻译结果写入 `destFilePath`。

3.  **调整主流程 (`processTask`)**：
    *   调用翻译函数时传入源路径和目标路径。
    *   确保 `labelMap`（用于同步侧边栏分类名称）应用在翻译后的目标文件上。

修改后，`zh-Hans` 将作为所有翻译的单一真实数据源。