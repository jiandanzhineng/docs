# 未选语言时提示切换

- 状态：Draft
- 日期：2026-08-29

## 摘要

未选语言且与浏览器 preferred 不一致时，出可关闭切换横幅。

## 用户要求

首次打开任意语言页，用户尚未选择语言，且当前页语言不是 `navigator.languages` 中第一个站点支持的语言时，显示建议切换横幅（不自动跳转）。横幅可关闭；关闭、点切换、用导航栏语言下拉均视为已选择，之后不再提示。`zh*`（含繁中）保持中文；`ja`/`es`/`de` 对应该语言；其他外语走英文。
